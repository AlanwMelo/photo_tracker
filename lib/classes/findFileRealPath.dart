/// Futura melhoria?

/*import 'dart:io';

class FileRealPath {
  final String fileName;

  FileRealPath(this.fileName);

  getFilePath() {
    List<FileSystemEntity> deviceDirs = Directory('/storage/').listSync().toList();
    for (var entity in deviceDirs){}
  }
}*/
